/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: "export",
    images: {
        unoptimized: true,
        // disableStaticImages: true,
    },
};

export default nextConfig;
